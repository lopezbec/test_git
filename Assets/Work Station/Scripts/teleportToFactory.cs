using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
//using UnityEngine.Experimental.UIElements;

public class teloportToFactory : MonoBehaviour
{
    public Button button;
    Transform station;
    Transform PlayerPosition;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if(ButtonListener.Pressed == true)
        //{
        //    teleportToFactory();
        //}
    }
    //public void teleportToFactory()
    //{
        //PlayerPosition = GameObject.Find("GrabbyGuy").GetComponent<Transform>();
        //station = GameObject.Find("ToolTable").GetComponent<Transform>();
        //PlayerPosition.position = new Vector3(station.position.x, 0, station.position.z);
    //}
}

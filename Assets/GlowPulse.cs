

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class GlowPulse : MonoBehaviour
{/*
    // public Material myMaterial;
    // Start is called before the first frame update

    CanvasRenderer rend;
    public Material glowShader;
    public Shader defaultShader;

    void Start()
    {
        rend = GetComponent<CanvasRenderer>();
        glowShader = Resources.Load("Glow Shader");
        defaultShader = Shader.Find("Test");


        pulse2();

    }

    // Update is called once per frame
    void Update()
    {


    }

    public void pulse1()
    {
        if (rend.GetMaterial() == glowShader) //if (rend.material.shader == glowShader)
        {
            rend.SetMaterial(defaultShader, 0); // rend.material.shader = defaultShader;
        }
        Invoke("pulse1", 2.0f);


    }

    public void pulse2()
    {
        if (!rend.GetMaterial() == glowShader)
        {
            SetMateral(glowShader, 0);

        }
        Invoke("pulse2", 2.0f);

    }
    */
}
